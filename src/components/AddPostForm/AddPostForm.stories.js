import React from "react";

import AddPostForm from "./AddPostForm";

export default {
  title: "Example/AddPostForm",
  component: AddPostForm,
};

const Template = () => <AddPostForm />;

export const Default = Template.bind({});
